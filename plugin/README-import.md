# How to import into Eclipse PDE

1. File → Import → Existing Projects into Workspace
2. Select this `plugin/` folder
3. Finish
4. Place `sqlite-jdbc-3.53.1.0.jar` as `lib/sqlite-jdbc.jar`
5. Project → Clean → Build All
6. Run As → Eclipse Application
